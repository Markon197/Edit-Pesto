// Bump this on every shipped change (however small) and add a line to
// CHANGELOG.md describing it. This is how the team tells, at a glance,
// whether the live site reflects the latest request.
export const APP_VERSION = "1";
export const VERSION_LABEL = `Version ${APP_VERSION}`;
